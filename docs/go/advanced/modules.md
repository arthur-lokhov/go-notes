---
title: Модули
---
# 📦 Модули в Go

## 🧠 Что такое модуль

!!! note "Модуль — это коллекция Go-пакетов, которая версионируется и распространяется как единое целое."

Модули являются основой системы управления зависимостями в Go и позволяют:

- использовать **семантическое версионирование**.
- изолировать зависимости между проектами.
- обеспечить **воспроизводимые сборки**.

Обычная структура модуля:

```bash
myproject/
├── go.mod         # Определение модуля и зависимостей
├── go.sum         # Контрольные суммы зависимостей
├── main.go        # Главный файл
├── internal/      # Внутренние подпакеты
├── mylib/         # Публичный пакет
└── vendor/        # (опционально) Копии всех зависимостей   
```

??? info "Отличие от старой модели (`$GOPATH`)."

    До **Go 1.11** код размещался в директории `$GOPATH/src`, и сборка была возможна только оттуда.

    Это приводило к:

    | Проблема                | Описание |
    |-------------------------|----------|
    | 🔄 Конфликты версий     | Разные проекты не могли использовать разные версии одной зависимости |
    | 🚫 Нет изоляции         | Все пакеты находились в одной структуре |
    | 🔧 Ручное управление     | Требовалась сторонняя утилита (`dep`, `glide`, `vendor/`) |

    Модульная система решает все эти проблемы.

---

## ❌ Поведение без `go.mod`

Если в проекте отсутствует `go.mod`, Go работает в **GOPATH-режиме**, который:

- требует, чтобы весь код был в `$GOPATH/src`.
- не позволяет импортировать подпакеты без полной структуры.
- не поддерживает версионирование и управление зависимостями.

!!! warning "Без go.mod вы не сможете импортировать даже подпакеты внутри проекта, так как Go не сможет их корректно идентифицировать."

---

## 📄 Файл `go.mod`

!!! note "Файл `go.mod` — это сердце модуля, которое описывает его."

Он определяет:

- имя модуля.
- минимальную версию Go.
- зависимости.
- локальные замены.
- исключения и аннулирования версий.

### 📌 Обязательные директивы

`module` - Определяет путь импорта модуля.

```gomod
module example.com/mymodule
```

| Тип       | Пример                          |
| --------- | ------------------------------- |
| Внешний   | `module github.com/myorg/mylib` |
| Локальный | `module mylib`                  |

`go` - Задает минимальную поддерживаемую версию Go.

```gomod
go 1.22
```

!!! info "Версия `go` также влияет на поведение компилятора: начиная с **Go 1.17**, изменяется поведение resolve-зависимостей и включаются фичи вроде generics (**1.18+**)."

`require` - Определяет зависимости и их версии придерживаясь MVS.

!!! note "Go использует Minimal Version Selection."

    Go использует стратегию минимального выбора версии — всегда выбирается самая старая версия зависимости, которая удовлетворяет требованиям всех модулей.

    Это обеспечивает воспроизводимость и предсказуемость сборок.

```gomod
require (
    github.com/google/uuid v1.3.0
    github.com/pkg/errors v0.9.1 // indirect
)
```

!!! info "`// indirect`"

    Зависимость, помеченная как `// indirect`, не используется напрямую в коде вашего модуля, а является зависимостью другой зависимости.
  
    ```gomod
    require (
        github.com/gorilla/mux v1.8.0 // Прямая зависимость
        github.com/pkg/errors v0.9.1 // indirect // Косвенная зависимость
    )
    ```

### ➕ Дополнительные директивы

`replace` - Позволяет заменить модуль на другую версию или путь.

!!! info "Работает только в локальном режиме. Никак не повлияет на распространение модуля и его конечный вариант."

```gomod
replace github.com/someone/lib => ../local/lib
replace github.com/lib v1.2.3 => github.com/fork/lib v1.2.4-fixed
```

Полезно при:

- разработке библиотек в локальном режиме.
- использовании форков.
- исправлении багов в сторонних библиотеках.

`exclude` - Исключает конкретные версии модуля.

```gomod
exclude github.com/bad/dependency v1.1.0
```

`retract` - Используется только в публикуемых модулях. Помечает версию как отозванную.

```gomod
retract (
    v1.2.0
    v1.3.0
)
```

!!! info "Данная запись не позволит использовать версии `1.2.0` и `1.3.0` данного модуля."

`toolchain` - Определяет версию набора инструментов Go.

```gomod
toolchain go1.22.5
```

| Ситуация | Что произойдет |
| ---- | ---- |
| `go 1.22` и `toolchain go1.22.5` | **Идеальный случай.** Код написан для языка Go 1.22 и будет собран **именно** инструментами версии `1.22.5`. Если у вас установлена другая версия Go, команда `go` автоматически скачает и использует `1.22.5` для этого проекта. |
| `go 1.22` и нет `toolchain` | Код будет собран любой версией инструментов, которая **не старше** `1.22`. Если у вас установлен Go `1.23`, он будет использован. Это может привести к тому, что у разных разработчиков проект собирается немного по-разному. |
| `go 1.22` и `toolchain go1.21.8` | **Ошибка.** Нельзя собрать код, требующий язык версии `1.22`, более старыми инструментами `1.21.8`. |

`godebug` - Позволяет устанавливать значения по умолчанию для переменных окружения `GODEBUG`.

```gomod
godebug tarinsecurepath=0
```

`tool` - Определяет зависимости инструментов.

```gomod
tool golang.org/x/tools/cmd/stringer
```

!!! tip "Фиктивный пакет `tools`."

    Обычно создается отдельный файл `tools.go`, чтобы Go не ругался, что эти пакеты не используются в коде.

    Например:

    ```bash
    myproject/
    ├── go.mod
    ├── go.sum
    ├── tools.go          # фиктивный импорт CLI-инструментов
    ├── main.go
    ├── internal/         # внутренние пакеты
    │   └── logic/
    │       └── service.go
    ├── mylib/            # публичные пакеты
    │   └── util.go
    └── generated/        # сгенерированный код (например, от stringer или mockgen)
    ```

    ```go
    //go:build tools
    // +build tools

    package tools

    import (
        _ "golang.org/x/tools/cmd/stringer"
    )
    ```
---

## 🔐 Файл `go.sum`

!!! note "Файл `go.sum` хранит **контрольные суммы загруженных зависимостей** для обеспечения целостности."

Пример содержимого:

```gosum
github.com/google/uuid v1.3.0 h1:A+l+3DR2k2H3j2X...
github.com/google/uuid v1.3.0/go.mod h1:TIyPZe4n...
```

`go.sum` обновляет свое содержимое после следующих команд:

- `go get`.
- `go mod tidy`.
- `go build` (если скачиваются новые зависимости).

!!! warning "Не редактируйте `go.sum` вручную — это приведет к ошибке валидации при следующей сборке."

---

## 🗃 Каталог `vendor`

Каталог `vendor/` содержит точные копии всех зависимостей проекта.

Данная директория создается командой ниже.

```bash
go mod vendor
```

Чтобы использовать пакеты из `vendor/` при сборке проекта укажите флаг `-mod=vendor`.

```bash
go build -mod=vendor
```

!!! tip "Используйте `-mod=vendor` в CI/CD, где нужен полный контроль и изоляция от внешнего мира."

### ✅ Плюсы

| Преимущество       | Описание                      |
| ------------------ | ----------------------------- |
| 📦 Изолированность | Все зависимости в репозитории |
| 🚫 Без сети        | Можно собирать офлайн         |
| 🔒 Предсказуемость | Зафиксированные версии        |

### ❌ Минусы

| Недостаток   | Причина                                 |
| ------------ | --------------------------------------- |
| 📈 Размер    | `vendor/` может занимать сотни мегабайт |
| 🔄 Поддержка | Нужно регулярно обновлять вручную       |

---

## 🌐 Прокси Go и приватные модули

!!! note "Go использует инфраструктуру, предоставляемую Google, для загрузки и проверки модулей."

С момента выхода **Go 1.13**, команда `go` по умолчанию использует три основных сервиса:

| Сервис             | Назначение                                                                  |
| ------------------ | --------------------------------------------------------------------------- |
| `proxy.golang.org` | Зеркало модулей (ускоряет загрузку, работает как кэш)                       |
| `sum.golang.org`   | Проверка контрольных сумм (гарантирует целостность и аутентичность модулей) |
| `index.golang.org` | Публикует поток новых версий модулей (для аналитики и автообновлений)       |

### 🔄 Как работает по умолчанию

Когда вы выполняете команду вроде: `go get github.com/some/module@v1.2.3`.

То Go:

1. Проверяет наличие модуля в `proxy.golang.org`.
2. Загружает модуль (если есть).
3. Сверяет контрольную сумму с `sum.golang.org`.
4. Если всё совпадает — кэширует результат и добавляет его в `go.sum`.

Это делает загрузку модулей:

- 📦 **надёжной** (проверка контрольных сумм).
- ⚡ **быстрой** (через кэш).
- 🔒 **воспроизводимой** (всегда одна и та же версия).

!!! example "Загрузка только их кэша (без доступа к внешнему интернету)."

    ```bash
    GOPROXY=https://proxy.golang.org,cached-only go mod download golang.org/x/tools@v0.1.0
    ```

    Если модуль ещё не был кэширован, произойдёт ошибка — но вы избежите долгих запросов.

### 🔐 Приватные модули: `GOPRIVATE`

Во многих проектах используется **приватный исходный код**, расположенный на GitHub, GitLab, Bitbucket или других системах.

По умолчанию Go использует публичную инфраструктуру (зеркало `proxy.golang.org`, базу контрольных сумм `sum.golang.org`), которая **не сможет получить доступ к приватным репозиториям** и вызовет ошибки.

✅ **Решение** — `GOPRIVATE`.

`GOPRIVATE` — это переменная окружения, в которой указываются пути к приватным модулям.

При его использовании Go будет:

- не использовать прокси (`proxy.golang.org`).
- не сверяться с базой контрольных сумм (`sum.golang.org`).
- обращаться напрямую к исходному коду по SSH или HTTPS.

```bash
go env -w GOPRIVATE=github.com/myorg/*
```

Если вы используете несколько доменов, то можно использовать данную запись.

```bash
go env -w GOPRIVATE=github.com/myorg/*,gitlab.company.com/*
```

!!! note "🔒 `GONOSUMDB` - отключение проверки контрольных сум."

    Можно отключить проверку контрольных сумм для приватных модулей, для этого нужно дополнительно установить:

    ```bash
    go env -w GONOSUMDB=github.com/myorg/*
    ```

---

## 📦 `go.work`

!!! note "Файл `go.work` позволяет работать с несколькими модулями одновременно, не изменяя `go.mod` каждого из них."

`go.work` определяет **рабочее пространство (workspace)**, которое объединяет несколько локальных модулей. Это особенно полезно, когда вы вносите изменения в зависимость и хотите протестировать их в основном проекте без использования директивы `replace`.

### 🚀 Как это работает

Когда в корне проекта есть файл `go.work`, Go-компилятор будет использовать локальные модули, указанные в нём, вместо версий, прописанных в `go.mod`.

Пример файла `go.work`:

```go
go 1.22

use (
    .
    ../mylibrary
)
```

В этом примере `use` указывает на два модуля:

- `.` — текущий модуль.
- `../mylibrary` — локальная зависимость.

### Основные команды

| Команда | Описание |
| --- | --- |
| `go work init` | Создаёт `go.work` в текущей директории. |
| `go work use [path]` | Добавляет модуль в `go.work`. |
| `go work edit` | Редактирует `go.work` (аналогично `go mod edit`). |
| `go work sync` | Синхронизирует зависимости из `go.mod` модулей рабочего пространства. |

!!! tip "Используйте `go.work` для локальной разработки, чтобы не коммитить временные `replace` директивы в `go.mod`."

---

## 📤 Публикация собственных модулей

!!! note "Публикация модуля — это процесс его версионирования и размещения в публичном репозитории."

Go не требует центрального репозитория пакетов (как npm или PyPI). Источником истины является ваш Git-репозиторий и его теги.

### 📜 Семантическое версионирование (SemVer)

Go строго придерживается **семантического версионирования**. Версия имеет формат `vX.Y.Z`:

| Часть | Название | Когда изменять | Пример |
| --- | --- | --- | --- |
| `X` | **Major** | Обратно несовместимые изменения (API ломается) | `v1.0.0` -> `v2.0.0` |
| `Y` | **Minor** | Добавление новой функциональности (обратно совместимо) | `v1.1.0` -> `v1.2.0` |
| `Z` | **Patch** | Исправление ошибок (обратно совместимо) | `v1.1.1` -> `v1.1.2` |

!!! warning "Версии `v0.x.x` считаются нестабильными и не дают гарантий обратной совместимости."

!!! info "Начиная с `v2`, мажорная версия должна быть частью пути модуля: `module github.com/myorg/mylib/v2`."

### 🚀 Процесс публикации

1.  **Подготовьте модуль**:
    - Убедитесь, что `go.mod` содержит корректный путь: `module github.com/myorg/mylib`.
    - Проект должен быть в публичном Git-репозитории.

2.  **Создайте Git-тег**:
    - Закоммитьте все изменения.
    - Создайте тег с префиксом `v`:

      ```bash
      git tag v1.0.0
      ```

3.  **Отправьте тег в репозиторий**:

    ```bash
    git push origin v1.0.0
    ```

После этого ваш модуль становится доступным для других разработчиков. Прокси `proxy.golang.org` автоматически обнаружит новую версию, когда кто-то попытается её загрузить через `go get`.

### 🔄 Обновление версии

Для выпуска новой версии (например, с исправлением бага):

1.  Внесите изменения в код.
2.  Закоммитьте их.
3.  Создайте и отправьте новый тег:

   ```bash
    git tag v1.0.1
    git push origin v1.0.1
    ```
