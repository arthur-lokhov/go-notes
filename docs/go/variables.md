---
title: Переменные
---
# 🔧 Переменные в Go

**Переменная** — это именованная область памяти, хранящая значение определенного типа. Тип переменной определяет множество допустимых значений и операций, которые можно над ней выполнять.

---

## ⚙️ Внутреннее устройство переменной

Когда в Go объявляется переменная, она резервирует область памяти, соответствующую типу значения, и связывает её с именем.
В зависимости от типа переменная:

- Может быть **значением** (value type) — хранящим данные напрямую.
- Может быть **ссылкой** (reference type) — указывающей на данные в куче.

В примитивных типах данные хранятся в стеке, а ссылочные типы указывают на выделенную память в куче.

---

## 🧾 Типы данных

Ниже приведена таблица с краткой сводкой по типам данных.

!!! note "Все переменные инициализируются нулевыми значениями (zero values), если значение явно не было задано, отличным от нулевого значения."

| Тип                                                      | Описание                        | Диапазон / особенности            | Нулевое значение | Категория   |
| -------------------------------------------------------- | ------------------------------- | --------------------------------- | ---------------- | ----------- |
| `int`, `int8`, `int16`, `int32`, `int64`                 | Целые числа                     | От -2ⁿ⁻¹ до 2ⁿ⁻¹-1                | `0`              | Примитивный |
| `uint`, `uint8`, `uint16`, `uint32`, `uint64`, `uintptr` | Беззнаковые числа               | От 0 до 2ⁿ-1                      | `0`              | Примитивный |
| `float32`, `float64`                                     | Числа с плавающей точкой        | IEEE-754                          | `0.0`            | Примитивный |
| `complex64`, `complex128`                                | Комплексные числа               | Состоит из двух float значений    | `0+0i`           | Примитивный |
| `byte`                                                   | Синоним для `uint8`             | Один байт                         | `0`              | Примитивный |
| `rune`                                                   | Синоним для `int32`             | Unicode-код поинт                 | `0`              | Примитивный |
| `bool`                                                   | Логическое значение             | `true` или `false`                | `false`          | Примитивный |
| `string`                                                 | Строка UTF-8                    | Иммутабельна, длина + указатель   | `""`             | Ссылочный   |
| `array`                                                  | Фиксированная длина             | Хранит значения напрямую          | `[0 ...]`        | Примитивный |
| `slice`                                                  | Динамическая последовательность | Структура: ptr, len, cap          | `nil`            | Ссылочный   |
| `map`                                                    | Ассоциативный массив            | Отображение ключ → значение       | `nil`            | Ссылочный   |
| `struct`                                                 | Составной тип                   | Поля разных типов                 | Поля = нули      | Примитивный |
| `interface`, `any`                                       | Абстрактный тип                 | typ + data (внутри)               | `nil`            | Ссылочный   |
| `error`                                                  | Интерфейс                       | Содержит метод `Error() string`   | `nil`            | Ссылочный   |
| `pointer` (`*T`)                                         | Указатель на значение           | Указывает на значение в памяти    | `nil`            | Ссылочный   |
| `function`                                               | Функция как значение            | Может быть `nil`                  | `nil`            | Ссылочный   |
| `chan`                                                   | Канал                           | Для коммуникации между горутинами | `nil`            | Ссылочный   |

---

## 📌 Объявление переменных

### 🧱 Классическое объявление

Ключевое слово `var` используется для объявления переменной.

```go
var name Type = value
```

Если значение указано, тип может быть **выведен автоматически**.

```go
var count = 42      // int
var enabled = true  // bool
```

А если же значение не задано - **тип указывать обязательно**.

```go
var port int
```

### 🧮 Множественное объявление

Можно объявлять несколько переменных одной строкой.

```go
var x, y, z int
```

!!! info "Все переменные должны иметь один и тот же тип"

    При множественном объявлении с явным указанием типа у всех переменных должен быть один и тот же тип.

    ```go
    var a, b, c int // ✅ корректно: все int
    var x, y string // ✅ корректно: все string
    var d int, e string // ❌ ошибка компиляции
    ```

### ⚡️ Краткое объявление (`:=`)

!!! note "Краткое объявление нельзя использовать на уровне пакета - только внутри функций."

В Go можно использовать краткую форму объявления.

```go
message := "hello"    // string
answer := 42          // int
```

Эквивалент коду ниже.

```go
var message = "hello"
var answer = 42
```

!!! danger "При кратком объявлении через `:=` важно создавать хотя бы одну новую переменную, чтобы избежать ошибки."

    ```go hl_lines="17"
    package main

    import (
        "errors"
        "fmt"
    )

    func main() {
        x, err := 20, errors.New("some error") // Обе переменные новые

        fmt.Println(x, err)

        z, err := 30, errors.New("some error 2") // Лишь одна переменная новая, но это работает

        fmt.Println(z, err)

        x, err := 40, errors.New("some error 3") // ❌ Ошибка: нету новых переменных
    }
    ```

---

## 🌀 Статическая и динамическая типизация (To Types)

Каждая переменная в Go имеет **статический тип**, известный на этапе компиляции.

!!! warning "Это означает, что нельзя присвоить переменной значение несовместимого типа."

```go
var x int // x имеет статический тип int
x = 42 // ✅
x = 4.2 // ❌ Ошибка: нельзя использовать 4.2 как значение int
```

Но переменные интерфейсного типа (`interface{}`, `any`) также обладают **динамическим типом** - фактическим типом хранимого значения во время выполнения.

```go
var x interface{} // x имеет статический тип interface{}, нулевое значение
x = 42 // x теперь содержит значение 42, динамический тип - int
x = "hello" // x теперь содержит строку, динамический тип - string
```

??? note "Почему это так работает"

    В Go **интерфейсная переменная (`interface{}`)** устроена не так, как простые переменные.

    Она логически состоит из двух частей:

    - **Type**: динамический тип значения(например, `int`, `string`, `*MyStruct`).
    - **Value**: само значение этого типа.

    Упрощенно структуру интерфейса можно представить следующим образом:

    ```go
    type iface struct {
        typ *itab           // Указатель на описание типа
        data unsafe.Pointer // Указатель на значение
    }
    ```

    Важно понимать, что несмотря на то что `interface{}` может хранить значение любого типа, он делает это не сам по себе, а через внутреннюю структуру `itab`.

    Когда ты пишешь `x = 42`, Go сохраняет:

    - `typ` -> указатель на `int` тип;
    - `data` -> значение `42`, приведенное к указателю.

    Когда ти пишешь `x = "hello"`, предыдущие данные теряются (сборщик мусора утилизирует, если нет других ссылок на эти данные), и переменная теперь указывает на:

    - `typ` -> `string`;
    - `data` -> `"hello"`.

    Про интерфейсы подробнее можно прочитать в соответствующей главе.

---

## 🌐 Глобальные переменные

Переменные, объявленные на уровне пакета (вне функций).

- Видимы во всех файлах пакета;
- Инициализируются до вызова `main()`;
- Могут использоваться в `init()` функциях.

```go
var globalVar = "I'm available everywhere in package"

func init() {
    fmt.Println(globalVar) // Доступно
}
```

---

## 🔄 Обмен значениями (Swap)

В Go можно легко поменять значения двух переменных местами.

```go hl_lines="2"
a, b := 1, 2
a, b = b, a
fmt.Println(a, b) // 2 1
```

---

## 🔍 Игнорируемое имя переменной (`_`)

В Go доступен специальный идентификатор `_`, называемый `blank identifier` (пустой идентификатор).
Он используется, когда **нужно получить, но не сохранять значение**.

В контексте объявления переменных `var` и `:=` он позволяет **отбросить значение**, не создавая именованную переменную.

!!! tip "То есть если функция возвращает два значения, но тебе нужно только одно — можно использовать `_`."

```go
value, _ := someFunc() // получаем только первый результат
var _, err = someFunc() // получаем только второй результат
```

!!! danger "Игнорирование всех значений"
    
    Если попытаться проигнорировать все значения, например:

    ```go hl_lines="8"
    package main

    func foo() (int, int) {
        return 1, 2
    }

    func main() {
        _, _ := foo() 
    }
    ```

    Программа не скомпилируется с ошибкой, которая сообщит нам, что с левой стороны не происходит считывание ни одного значения.

---

## 🔨 `new`: выделение памяти под значение (To Functions)


Функция [`new`](https://pkg.go.dev/builtin#new) выделяет память под значение указанного типа и возвращает **указатель** на нулевое значение этого типа.

```go
p := new(int)
fmt.Println(*p) // 0
*p = 34
fmt.Println(*p) // 34
```

!!! note "Функция `new` применима к любому типу (и к примитивным, и к ссылочным)."

```go
x := new(int)               // *int, содержит 0
s := new([]int)             // *[]int, содержит nil slice
m := new(map[int]string)    // *map[int]string, содержит nil map
```

!!! danger "Функция `new` не инициализирует внутреннюю структуру типов"

    Важно знать, что `new(slice|map|chan)` только создает указатель на `nil`, не инициализируя внутреннюю структуру.

    Чтобы получить рабочий slice|map|chan, нужно использовать `make`.

---

## 🧰 `make`: инициализация ссылочных типов (To Functions)

!!! note "Функция `make` работает только со срезами, картами и каналами."

Функция [`make`](https://pkg.go.dev/builtin#make) - это **встроенная функция компилятора**, жестко органиченная на работу **только с тремя встроенными типами**.

```go
s := make([]int, 10)
m := make(map[string]int)
c := make(chan int)
```

!!! note "`make` возвращает **инициализированный** объект, а не указатель в отличии от `new`."

```go hl_lines="5 7"
package main

func main() {
	s1 := new([]int)
    fmt.Println(s1, *s1)    // Ссылка: &[], Значение []
	s2 := make([]int, 5)
	fmt.Println(s2)         // Значение: [0 0 0 0 0]
}
```

!!! info "Пока объект не проинициализирован в него нельзя писать данные."

    Не представляется возможным поместить в slice|map|chan значение пока они не проинициализированы.

    Исключением является `append` для срезов, он безопасен при работе с `nil` срезом.

    ```go
    var s []int // nil int slice
    s = append(s, 10) // работает
    fmt.Println(s) // [10]

    // s[0] = 5 // ❌Ошибка: значение за пределами массива
    ```

---

## 📛 Константы

Константы (`const`) - это **неизменяемые значения**, определяемые во время компиляции:

```go
const Pi = 3.1415
const Greeting = "Hello"
```

Тип можно указать явно или вывести автоматически.

```go
const x int = 10
const y = 20
```

---

## 🔢 `iota` — автоинкремент для констант

`iota` — это **встроенный идентификатор**, используемый в блоках `const` для **генерации последовательностей**.

```go hl_lines="2"
const (
    A = iota    // 0
    B           // 1
    C           // 2
)
```

### 📌 Общая модель

- `iota` - счетчик, **начинающийся с 0 в каждом блоке `const`**;
- На **каждой новой строке** внутри блока `iota` увеличивается на 1;
- Каждая строка - это **независимая вычисляемая инструкция**, даже если она "визуально" ничего не содержит (повторение предыдущего выражения).

### ⚙️ Использование `_` в блоках с `iota`

`_` **не влияет на `iota`**: счетчик продолжает увеличиваться, даже если значение не используется.

```go hl_lines="2"
const (
    _ = iota    // iota = 0, значение отброшено
    A           // iota = 1, A = 1
    B           // iota = 2, B = 2
)
```

!!! info "Если использовать `_` в середине блока `const`, то надо будет снова затриггерить `iota`."

```go hl_lines="4"
const (
    A = iota    // iota = 0, A = 0
    _           // 1, значение отброшено
    B = iota    // iota = 2, B = 2
    C           // iota = 3, C = 3
)
```

### 🔄 Поведение при комбинированных выражениях

Когда ты пишешь несколько выражений на одной строке, `iota` будет иметь **одно и то же значение** во всей этой строке:

```go
const (
    A, B = iota, iota   // 0, 0
    C, D = iota, iota   // 1, 1
)
```

!!! warning "Количество констант и инициализаторов `iota` должно совпадать."

    ```go
    const (
        A, B    = iota, iota // 0, 0
        C, D                 // 1, 1
        // E, F, G              // ❌ Ошибка: нету значения для инициализации G
    )
    ```

### ⚠️ Перезапись и комбинирование значений

Можно свободно **смешивать выражения с `iota` и без** - но `iota` продолжает увеличиваться **по строкам**, а не значимости выражений.

```go
const (
    A = iota            // iota = 0, A = 0
    B                   // iota = 1, B = 1 (повторяет значение A)
    C = 100             // iota = 2, C = 100
    D                   // iota = 3, D = 100 (повторяет значение C)
    E = iota * 2        // iota = 4, E = 8
    F, G = iota, iota   // iota = 5, F = 5, G = 5
    H = "static"        // iota = 6, H = "static"
    I                   // iota = 7, I = "static" (повторяет значение H)
)
```

### 🛠 Использование `iota` в битовых масках

`iota` можно использовать вместе с побитовыми сдвигами.

Например, для расчета памяти:

```go
const (
    _ = iota                // 0 (пропускаем)
    KB = 1 << (10 * iota)   // 1 << 10 = 1024
    MB                      // 1 << 20 = 1048576
    GB                      // 1 << 30 = 1073741824
)
```

Или для управления флагами:

```go
const (
    FlagNone = 0
    FlagRead = 1 << iota // 1 << 0 = 1
    FlagWrite            // 1 << 1 = 2
    FlagExec             // 1 << 2 = 4
    FlagAll  = FlagRead | FlagWrite | FlagExec // 7
)
```

---

## 🧬 Внутреннее устройство переменных в компиляторе Go

### 📦 Интерфейс `Object`

!!! tip "Исходный код"

    https://go.googlesource.com/go/+/refs/heads/master/src/go/types/object.go    

Внутри компилятора Go все именованные сущности, будь то переменные, функции, константы или типы, представлены с помощью интерфейса `types.Object`.

**Object** — это унифицированный интерфейс, который описывает любую именованную сущность в Go-коде.
Он используется в таблицах областей видимости (Scope) и позволяет работать с любым объектом абстрактно, не зная его конкретного типа (переменная, тип, функция и т.д.).

```go
type Object interface {
	Parent() *Scope
	Pos() token.Pos
	Pkg() *Package
	Name() string
	Type() Type
	Exported() bool
	Id() string
	String() string
}
```

| Метод        | Назначение                                                                     |
| ------------ | ------------------------------------------------------------------------------ |
| `Parent()`   | Область видимости, в которой находится объект (например, функция, файл, пакет) |
| `Pos()`      | Позиция в исходнике, где объект был объявлен                                   |
| `Package()`  | Пакет, которому принадлежит объект (может быть `nil`, если объект глобальный)  |
| `Name()`     | Имя объекта, как оно написано в коде                                           |
| `Type()`     | Тип объекта (`types.Type`) — например, `int`, `[]string`, `MyStruct`           |
| `Exported()` | `true`, если объект экспортируется (начинается с заглавной буквы)              |
| `Id()`       | Уникальный идентификатор с учетом пакета                                       |
| `String()`   | Отладочное представление                                                       |

!!! note "Объект - это интерфейс"

    **Важно**: Конкретные реализации (переменные, функции, типы и т.д.) реализуют этот интерфейс по-своему.

### 🪙 Структура `Var`

!!! example "Исходный код"

    https://cs.opensource.google/go/go/+/master:src/go/types/object.go;l=334

`types.Var` — реализация интерфейса Object, представляющая переменные во всех контекстах: локальные переменные, глобальные (package-level), параметры функций, результаты, ресиверы методов, а также поля структур.

```go
type Var struct {
	object
	origin   *Var
	kind     VarKind
	embedded bool
}
```

| Поле       | Тип       | Назначение                                                                                          |
| ---------- | --------- | --------------------------------------------------------------------------------------------------- |
| `object`   | `object`  | Встроенная структура, реализующая интерфейс `Object` (общее поведение всех именованных сущностей)   |
| `origin`   | `*Var`    | Исходная переменная, от которой произошла текущая (например, при подстановке типов в generics)      |
| `kind`     | `VarKind` | Вид переменной — определяет контекст, в котором она используется (поле, параметр, локальная и т.д.) |
| `embedded` | `bool`    | Флаг, что переменная — встроенное поле (в этом случае имя переменной совпадает с именем типа)       |

#### 🧷 Тип `VarKind`

!!! example "Исходный код"

    https://cs.opensource.google/go/go/+/master:src/go/types/object.go;l=342

Тип `VarKind` — это перечисление (enum), указывающее контекст, в котором используется переменная.
Это полезно при анализе AST или в инспекциях IDE (например, отличить параметр от поля структуры).

```go
type VarKind uint8

const (
	_          VarKind = iota
	PackageVar
	LocalVar
	RecvVar
	ParamVar
	ResultVar
	FieldVar
)
```

| Значение     | Описание                                                    |
| ------------ | ----------------------------------------------------------- |
| `PackageVar` | Глобальная переменная, объявленная на уровне пакета         |
| `LocalVar`   | Локальная переменная внутри функции или блока               |
| `RecvVar`    | Ресивер метода (`r` в `func (r T) Method()`)                |
| `ParamVar`   | Входной параметр функции                                    |
| `ResultVar`  | Результирующее значение функции (в именованных результатах) |
| `FieldVar`   | Поле структуры (`type T struct { X int }`)                  |

### 🧱 Структура `Const`

!!! example "Исходный код"

    https://cs.opensource.google/go/go/+/master:src/go/types/object.go;l=261

`types.Const` — реализация Object для констант, таких как const `Pi = 3.14`.

```go
type Const struct {
	obj
	val constant.Value // значение константы
}
```

| Поле  | Назначение                                                                                     |
| ----- | ---------------------------------------------------------------------------------------------- |
| `obj` | Базовая реализация `Object`                                                                    |
| `val` | Значение константы — представлено через `constant.Value` (поддерживает типы int, float и т.д.) |

