---
title: Работа с типами
---

# 🛠️ Работа с типами в Go

В Go, помимо использования встроенных типов, можно создавать свои собственные, а также гибко работать с типами в контексте интерфейсов.

Этот раздел посвящен ключевым механикам управления типами.

---

## 🏭 Пользовательские типы

Go позволяет создавать новые типы на основе существующих.

Есть два способа: **определение нового типа** и **создание псевдонима типа**.

### 🧱 Новый тип

Ключевое слово `type` создает **совершенно новый, отдельный тип**, который несовместим с его базовым типом, даже если их внутренняя структура идентична.

Это позволяет вводить строгую семантическую проверку на этапе компиляции.

**Синтаксис:**

```go
type NewTypeName UnderlyingType
```

!!! example "Пример: Celsius и Fahrenheit"

    ```go hl_lines="5 6 12 15"
    package main

    import "fmt"

    type Celsius float64
    type Fahrenheit float64

    func main() {
        var c Celsius = 25.0
        var f Fahrenheit = 77.0

        // fmt.Println(c == f) // ❌ Ошибка: несовместимые типы (mismatched types)
        
        // Преобразование необходимо для сравнения или присваивания
        fmt.Println(c == Celsius(f)) // ✅ Корректно, но семантически неверно без формулы
    }
    ```

    Хотя и `Celsius`, и `Fahrenheit` основаны на `float64`, для компилятора это разные типы.

    Это позволяет создавать более строгие и семантически верные API, предотвращая случайное смешивание разных сущностей.

!!! success "Рекомендация"

    Используйте определение нового типа, когда вы хотите:

    - Придать существующему типу новый семантический смысл (например, `type UserID string`).
    - Добавить методы к простому типу (например, к `string` или `int`).
    - Обеспечить типобезопасность, чтобы случайно не смешать переменные разного назначения.

### 🖇️ Псевдоним типа

Псевдоним — это **второе имя для существующего типа**.
Он не создает новый тип, а лишь дает старому новое имя.

Типы полностью взаимозаменяемы, и компилятор считает их одним и тем же.

**Синтаксис:**
```go
type AliasName = ExistingType
```

!!! example "Пример: `byte` и `rune`"

    В стандартной библиотеке Go `byte` и `rune` являются псевдонимами:

    ```go
    type byte = uint8
    type rune = int32
    ```

    Это означает, что `byte` и `uint8` — это один и тот же тип.

    Псевдонимы используются для повышения читаемости кода, не вводя при этом новых типовых ограничений.

!!! tip "Совет"

    Используйте псевдонимы типов для:

    - Упрощения длинных или сложных имен типов (`type Users = map[string]*User`).
    - Постепенного рефакторинга, когда вы хотите переименовать тип, но сохранить обратную совместимость.

---

## 🔄 Преобразование типов

Преобразование типов — это явная операция изменения типа значения.

В Go нет неявных преобразований между разными типами, что делает код более предсказуемым.

**Синтаксис:**

```go
T(v) // Преобразовать значение v к типу T
```

!!! info "Правила"

    1.  Преобразование возможно между числовыми типами (`int`, `float64` и т.д.).
    2.  Преобразование возможно между строками и срезами байт/рун.
    3.  Преобразование возможно между типами, у которых **одинаковый базовый тип (underlying type)**.

```go

var i int = 42
var f float64 = float64(i) // ✅

type Age int
var myAge Age = 30
var myInt int = int(myAge) // ✅ (одинаковый базовый тип int)
```

!!! warning "Потеря данных"

    При преобразовании от более "широкого" типа к более "узкому" возможна потеря данных.

    Компилятор не предотвратит это, поэтому ответственность лежит на разработчике.

    ```go
    var f float64 = 3.14
    var i int = int(f) // i будет равно 3 (дробная часть отброшена)

    var x int = 257
    var y uint8 = uint8(x) // y будет равно 1 (257 % 256)
    ```

---

## 📖 Базовый тип

Концепция базового типа является центральной для системы типов Go.

**Базовый тип** — это то, чем тип является "на самом деле", если "развернуть" все определения пользовательских типов.

!!! info "Определение базового типа"

    У предопределенных типов (`int`, `string`, `bool` и т.д.) базовый тип — это они сами.

    У пользовательского типа, созданного через `type T1 T2`, базовый тип такой же, как у `T2`.

    У анонимных типов (например, `struct { Name string }`) базовый тип — это они сами.

!!! example "Примеры"

    ```go
    type Age int // Базовый тип для Age - это int

    type Number Age // Базовый тип для Number - это тоже int

    type User struct { Name string } // Базовый тип - struct { Name string }
    
    type Users []User // Базовый тип - []User
    ```

Базовый тип важен для двух вещей:

1.  **Преобразование типов**: вы можете преобразовывать значения между двумя типами, если у них одинаковый базовый тип.
2.  **Присваиваемость**: в некоторых случаях значения можно присваивать между типами с одинаковым базовым типом (см. ниже).

---

## 🆔 Идентичность типов

Два типа в Go считаются **идентичными**, если они соответствуют одному из следующих правил.

Идентичность типов является ключевым фактором, определяющим, можно ли присвоить одно значение другому без явного преобразования.

!!! example "Правила и примеры идентичности"

    === "Предопределенные типы"

        Два типа идентичны, если они являются одним и тем же предопределенным типом (`int`, `string` и т.д.).

        ```go
        package main

        func main() {
            var a int
            var b int
            // Типы 'a' и 'b' идентичны, так как оба - предопределенный тип 'int'.
            // Поэтому присваивание возможно.
            a = b
        }
        ```

    === "Псевдонимы типов"

        Два типа идентичны, если они являются псевдонимами (`type T1 = T2`) одного и того же типа.

        ```go
        package main

        func main() {
            type Text = string
            type Message = string

            var t Text = "hello"
            var m Message
            
            // ✅ Корректно. Типы Text и Message идентичны, 
            // так как оба являются псевдонимами string.
            m = t 
        }
        ```

    === "Именованные типы"

        Два типа идентичны, если это именованные типы, созданные **одним и тем же** объявлением `type`.

        ```go
        package main

        func main() {
            // Одно объявление -> типы идентичны
            type Age int
            var a1 Age
            var a2 Age
            a1 = a2 // ✅ Корректно. Типы a1 и a2 идентичны.

            // Разные объявления -> типы НЕ идентичны
            type Kilogram int
            type Gram int
            var kg Kilogram
            var g Gram
            // kg = g // ❌ Ошибка компиляции: cannot use g (type Gram) as type Kilogram
        }
        ```

        Это правило показывает фундаментальное различие между псевдонимом и определением нового типа.

    === "Анонимные структуры"

        Два анонимных структурных типа идентичны, если у них одинаковая последовательность полей: те же имена, те же типы и те же теги.

        Порядок полей имеет значение.

        ```go
        package main

        func main() {
            var s1 struct { Name string; Age int `json:"age"` }
            var s2 struct { Name string; Age int `json:"age"` }
            s1 = s2 // ✅ Корректно. Типы идентичны.

            // Порядок полей разный -> типы НЕ идентичны
            var s3 struct { Age int `json:"age"`; Name string }
            // s1 = s3 // ❌ Ошибка компиляции.

            // Теги разные -> типы НЕ идентичны
            var s4 struct { Name string; Age int `json:"AGE"` }
            // s1 = s4 // ❌ Ошибка компиляции.
        }
        ```

    === "Анонимные функции"

        Два типа-функции идентичны, если у них одинаковые сигнатуры: одинаковое количество и типы параметров и результатов.

        Имена параметров не имеют значения.

        ```go
        package main
        
        func main() {
            var f1 func(int, string) bool
            var f2 func(int, string) bool
            f1 = f2 // ✅ Корректно. Типы идентичны.

            // Имена параметров не важны
            var f3 func(a int, b string) bool
            f1 = f3 // ✅ Корректно.

            // Разное количество результатов -> типы НЕ идентичны
            var f4 func(int, string) (bool, error)
            // f1 = f4 // ❌ Ошибка компиляции.
        }
        ```

---

## ✅ Присваиваемость

**Присваиваемость** определяет, можно ли значение `x` присвоить переменной типа `T` (т.е. `var v T = x`).

Это происходит без явного преобразования типов.

!!! example "Правила и примеры присваиваемости"

    === "Идентичные типы"

        Значение `x` можно присвоить переменной `T`, если их типы идентичны.

        ```go
        package main

        func main() {
            // Типы идентичны (оба - предопределенный тип int)
            var a int
            var b int = 10
            a = b // ✅ Корректно

            // Типы идентичны (оба - псевдонимы string)
            type T1 = string
            type T2 = string
            var v1 T1 = "hello"
            var v2 T2
            v2 = v1 // ✅ Корректно
        }
        ```

    === "Одинаковый базовый тип"

        Присваивание возможно, если типы `x` и `T` имеют одинаковый базовый тип, и **хотя бы один из них не является именованным типом**.
        
        Именованные типы — это те, что объявлены через `type Name T`.

        Неименованные — это литералы (`[]int{1,2}`, `struct{}{}`).

        ```go
        package main

        func main() {
            type MySlice []int
            var s MySlice

            // Тип `[]int{1, 2}` - неименованный.
            // Его базовый тип `[]int` совпадает с базовым типом `MySlice`.
            // Присваивание возможно.
            s = []int{1, 2} // ✅ Корректно

            type Age int
            var myAge Age = 30

            var myInt int = 40
            // myAge = myInt // ❌ Ошибка: оба типа (Age и int) являются именованными.
            _ = myAge
            _ = myInt
        }
        ```

        *Примечание: константы, например `30`, являются "нетипизированными" и могут быть присвоены любой переменной совместимого типа.*

    === "Реализация интерфейса"

        Значение можно присвоить переменной интерфейсного типа, если тип значения реализует этот интерфейс.

        ```go
        package main
        
        import "fmt"

        type Speaker interface {
            Speak() string
        }

        type Dog struct{}
        func (d Dog) Speak() string { return "Woof!" }

        type Cat struct{}
        func (c Cat) Speak() string { return "Meow!" }

        func main() {
            var s Speaker
            
            // Тип Dog реализует Speaker
            s = Dog{} // ✅ Корректно
            fmt.Println(s.Speak())

            // Тип Cat тоже реализует Speaker
            s = Cat{} // ✅ Корректно
            fmt.Println(s.Speak())
        }
        ```

    === "Присваивание `nil`"

        Предопределенное значение `nil` можно присвоить переменной любого из следующих типов: указатель, функция, слайс, мапа, канал или интерфейс.

        ```go
        package main

        func main() {
            var p *int
            p = nil // ✅ Указатель

            var s []int
            s = nil // ✅ Слайс

            var m map[string]int
            m = nil // ✅ Мапа

            var f func()
            f = nil // ✅ Функция

            var c chan int
            c = nil // ✅ Канал

            var i interface{}
            i = nil // ✅ Интерфейс
            
            _, _, _, _, _, _ = p, s, m, f, c, i
        }
        ```

---

## 🌀 Динамический тип

Каждая переменная в Go имеет **статический тип**, известный на этапе компиляции.

!!! warning "Это означает, что нельзя присвоить переменной значение несовместимого типа."

    ```go
    var x int // x имеет статический тип int
    x = 42 // ✅
    // x = 4.2 // ❌ Ошибка: нельзя использовать 4.2 как значение int
    ```

Но переменные интерфейсного типа (`interface{}` или `any`) также обладают **динамическим типом** — фактическим типом хранимого значения во время выполнения.

```go
var x interface{} // x имеет статический тип interface{}, нулевое значение
x = 42 // x теперь содержит значение 42, динамический тип - int
x = "hello" // x теперь содержит строку, динамический тип - string
```

Значение типа `interface{}` (или `any`) имеет статический тип (сам интерфейс) и динамический тип (конкретный тип значения, которое в нем хранится).

Чтобы работать с этим конкретным значением, нужно "извлечь" его из интерфейса.
Для этого существуют утверждения и переключатели типов.

??? note "Внутреннее устройство интерфейса и скрытые аллокации"

    !!! warning "Без знания о том, что такое интерфейсы может быть трудным к пониманию."

    В Go **интерфейсная переменная** (будь то пустой интерфейс `interface{}`/`any` или интерфейс с методами) устроена не так, как простые переменные.

    Она логически состоит из двух частей:

    -   **Динамический тип (`_type` / `itab`)**: Описывает фактический тип значения, хранящегося в интерфейсе.
    -   **Динамическое значение (`data`)**: Указатель на само значение.

    В рантайме Go используются две основные структуры для представления интерфейсных значений:

    *   **`runtime.eface`** (для пустого интерфейса `interface{}` или `any`):

        ```go
        type eface struct {
            _type *_type        // Указатель на описание типа (runtime._type)
            data  unsafe.Pointer // Указатель на значение
        }
        ```

    *   **`runtime.iface`** (для интерфейсов с методами):

        ```go
        type iface struct {
            itab  *itab         // Указатель на таблицу интерфейса (runtime.itab)
            data  unsafe.Pointer // Указатель на значение
        }
        ```

    **Скрытые аллокации и производительность:**

    Когда значение **значимого типа** (например, `int`, `struct`) присваивается интерфейсной переменной, компилятор **создает копию этого значения в куче**, и поле `data` в `eface`/`iface` будет указывать на эту копию.

    Это происходит потому, что интерфейс должен хранить указатель на значение, а значимые типы обычно хранятся на стеке.

    ```go
    var i int = 42
    var a any = i // Копия 42 аллоцируется в куче, 'a.data' указывает на неё.
    ```

    Это является **скрытой аллокацией**, которую сеньор должен учитывать при написании высокопроизводительного кода, особенно в циклах или при работе с большим количеством мелких значений, передаваемых через интерфейсы.

    Избыточные аллокации в куче увеличивают нагрузку на сборщик мусора и могут снижать производительность.

    **`runtime.itab`**:

    !!! info "Исходный код `runtime.itab` см. в (go/go) 👉 [**runtime2.go**](https://cs.opensource.google/go/go/+/master:src/runtime/runtime2.go;l=400)."

    `runtime.itab` — это структура в рантайме, которая описывает конкретную реализацию интерфейса для определенного типа.

    Она содержит:

    -   `_type`: тип, реализующий интерфейс.
    -   `inter`: сам интерфейс.
    -   `fun`: массив указателей на методы, реализующие интерфейс.

    `itab` создается рантаймом при первом присвоении конкретного типа интерфейсу и кэшируется для последующего использования, что оптимизирует поиск методов.

    Про интерфейсы подробнее можно прочитать в соответствующей главе.

### 🔍 Утверждение типа

**Утверждение типа** — это операция, которая проверяет, что значение в интерфейсе имеет определенный конкретный тип, и извлекает это значение.

**Синтаксис:**

```go
value, ok := i.(T)
```

- `i` — переменная интерфейсного типа.
- `T` — тип, к которому мы пытаемся привести `i`.
- `value` — значение типа `T`, если утверждение успешно.
Если неуспешно, `value` будет нулевым значением для типа `T`.
- `ok` — булево значение: `true`, если утверждение успешно, иначе `false`.

```go
var i any = "hello"

s, ok := i.(string)
if ok {
    fmt.Println(s) // "hello"
}

f, ok := i.(float64)
if !ok {
    fmt.Println("i is not a float64") // Сработает этот блок
    fmt.Printf("f has the zero value: %v\n", f) // f has the zero value: 0
}
```

!!! danger "Паникующая форма утверждения"

    Существует также форма без второго возвращаемого значения (`value := i.(T)`).
    Ее стоит использовать, только если вы на 100% уверены в типе.

    **Если тип окажется другим, программа упадет с паникой.**

    ```go
    var i any = "hello"
    s := i.(string) // ✅ Успешно
    // f := i.(float64) // ❌ Паника: interface conversion: interface {} is string, not float64
    ```

### 🔀 Переключатель типов

Это идиоматичный и безопасный способ проверить переменную интерфейсного типа на соответствие сразу нескольким типам.

Он похож на обычный `switch`, но использует специальную форму `i.(type)`.

**Синтаксис:**

```go
switch v := i.(type) {
case T1:
    // ... код для типа T1, v имеет тип T1
case T2, T3:
    // ... код для типов T2 или T3, v имеет тип i (interface{})
default:
    // ... если ни один тип не подошел, v имеет тип i (interface{})
}
```

!!! note "Особенность `case` с несколькими типами"

    Если в `case` перечислено несколько типов, то переменная `v` внутри этого блока будет иметь исходный интерфейсный тип, а не конкретный тип.

    Это связано с тем, что компилятор не знает, какой именно из перечисленных типов (`T2` или `T3`) сработал.

!!! example "Пример использования"

    ```go
    func do(i any) {
        switch v := i.(type) {
        case int:
            fmt.Printf("Это целое число: %d\n", v)
        case string:
            fmt.Printf("Это строка: %q (длина %d)\n", v, len(v))
        case bool, float64:
            fmt.Printf("Это bool или float64, тип: %T\n", v)
        default:
            fmt.Printf("Неизвестный тип: %T\n", v)
        }
    }
    ```

---

## 🧬 Внутреннее устройство

В Go компилятор использует пакет `go/types` для представления и проверки типов.
Это основа для статической типизации языка.

На этом уровне каждый тип в Go представлен как объект, реализующий интерфейс `Type`.

### 🏷️ Интерфейс `Type`

!!! info "Исходный код `types.Type` см. в (go/go) 👉 [**type.go**](https://cs.opensource.google/go/go/+/master:src/go/types/type.go)."

`types.Type` — это центральный интерфейс в пакете `go/types`, который представляет **любой тип** в Go.

Все конкретные представления типов (базовые, именованные, структуры, интерфейсы и т.д.) реализуют этот интерфейс.

```go
// A Type represents a type of Go.
// All types implement the Type interface.
type Type interface {
	// Underlying returns the underlying type of a type.
	Underlying() Type

	// String returns a string representation of a type.
	String() string
}
```

| Метод        | Назначение                                                                                                                                                           |
| :----------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Underlying()` | Возвращает **базовый (underlying) тип**. Например, для `type MyInt int` базовым типом будет `int`. Этот метод "разворачивает" все именованные типы до их основы. |
| `String()`   | Возвращает строковое представление типа, которое используется, например, в сообщениях об ошибках компиляции.                                                          |

### 🔢 Структура `Basic`

!!! info "Исходный код `types.Basic` см. в (go/go) 👉 [**basic.go**](https://cs.opensource.google/go/go/+/master:src/go/types/basic.go)."

`types.Basic` — это структура, реализующая `Type` и представляющая **базовые, предопределенные типы** Go, такие как `int`, `string`, `bool`, `float64`, а также их нетипизированные аналоги (`UntypedInt`).

```go
// A Basic represents a basic type.
type Basic struct {
	kind BasicKind
	info BasicInfo
	name string
}
```

| Метод    | Назначение                                                                                                                                                           |
| :------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Kind()`   | Возвращает "вид" базового типа (например, `Int`, `String`, `Bool`) в виде константы `BasicKind`.                                                                       |
| `Info()`   | Возвращает битовую маску `BasicInfo` со свойствами типа (например, `IsInteger`, `IsComplex`, `IsUnsigned`). Это позволяет компилятору быстро проверять категории типов. |
| `Name()`   | Возвращает имя типа в виде строки (например, `"int"`, `"string"`).                                                                                                   |

### 📛 Структура `Named`

!!! info "Исходный код `types.Named` см. в (go/go) 👉 [**named.go**](https://cs.opensource.google.go/go/+/master:src/go/types/named.go)."

`types.Named` — это ключевая структура, представляющая **именованные типы**, определенные пользователем (например, `type MyInt int` или `type User struct { ... }`).

Она связывает имя типа (`TypeName`) с его базовым типом (`underlying type`) и привязанными к нему методами.

Эта структура также содержит сложную логику для ленивой загрузки данных (lazy loading) и поддержки дженериков (инстанцирование типов).

```go
// A Named represents a named (defined) type.
type Named struct {
	obj        *TypeName
	underlying Type
	tparams    *TypeParamList // type parameters
	methods    []*Func        // methods
	// ... и другие поля для ленивой загрузки, инстанцирования и кеширования
}
```

| Метод          | Назначение                                                                                                                                                           |
| :------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Obj()`        | Возвращает объект `*TypeName`, представляющий объявление типа (`type MyType ...`).                                                                                    |
| `Underlying()` | Возвращает базовый (underlying) тип, рекурсивно разрешая все промежуточные именованные типы.                                                                          |
| `NumMethods()` | Возвращает количество явных методов, привязанных к типу.                                                                                                             |
| `Method(i)`    | Возвращает `i`-тый метод типа.                                                                                                                                       |
| `TypeParams()` | Возвращает список параметров-типов для дженериков (например, `[T any]`).                                                                                             |
| `TypeArgs()`   | Возвращает список аргументов-типов для инстанцированного дженерика (например, `[int]`).                                                                              |
| `Origin()`     | Для инстанцированного типа возвращает исходный дженерик-тип.                                                                                                         |

!!! abstract "Структура TypeName"

    `TypeName` — это конкретная реализация `Object`, представляющая имя **объявленного типа**. Это не сам тип, а именно его имя и место объявления."

    ```go
    // A TypeName represents a declared type name.
    // The associated Object is of type *TypeName.
    type TypeName struct {
        object
    }
    ```

    Простыми словами, `TypeName` — это "идентификатор" типа в коде, а `Named` — это полная модель этого типа со всеми его свойствами, методами и т.д.

??? note "Про TypeParam и TypeParamList"

    !!! info "Исходный код `types.TypeParam` см. в (go/go) 👉 [**typeparam.go**](https://cs.opensource.google.go/go/+/master:src/go/types/typeparam.go)."

    С появлением дженериков в Go 1.18, в `go/types` были добавлены структуры для их представления.

    `TypeParam` представляет один **параметр-тип** в объявлении дженерика (например, `T` в `func Print[T any](s T)`).

    ```go
    // A TypeParam represents the type of a type parameter.
    type TypeParam struct {
        check *Checker
        id    uint64
        obj   *TypeName // Имя параметра-типа (например, 'T')
        index int       // Индекс в списке параметров (0, 1, ...)
        bound Type      // Ограничение типа (constraint)
    }
    ```

    - **`Obj()`**: Возвращает `*TypeName`, связанный с параметром-типом.
    - **`Constraint()`**: Возвращает `Type`, представляющий ограничение (например, `any` или `io.Reader`).

    `TypeParamList` — это просто упорядоченный список таких параметров-типов.

    ```go
    // TypeParamList holds a list of type parameters.
    type TypeParamList struct {
        tparams []*TypeParam
    }
    ```

    Эти структуры позволяют компилятору проверять корректность использования дженериков, выполнять инстанцирование (подстановку конкретных типов) и проверять соответствие типов ограничениям.

